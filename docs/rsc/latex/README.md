These LaTeX files are retained only as historical layout references for the browser-native packet renderer in `docs/utils/pdf/packetDocuments.js`.

Runtime packet compilation does not fetch these files and does not require Pandoc or LaTeX.
